# aktenfuchs
Aktenfuchs is a local, privacy-first document assistant for OCR-ready PDFs. It uses a local LLM via Ollama to summarize documents, extract metadata, suggest safe filenames, and prepare files for reviewed archival.
